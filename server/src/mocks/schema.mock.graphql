# import Match from '../generated/prisma.graphql'
# import Query from '../schema.graphql'
# import Mutation from '../schema.graphql'
# import AllPlayersMeta from '../schema.graphql'

type Player {
  id: ID!
  email: String!
  facebookId: String!
  firstName: String!
  lastName: String!
  matches: [Match!]!
  wins: Int!
  losses: Int!
}
